import { EstablecimientoEntity } from './establecimiento.entity';

describe('EstablecimientoEntity', () => {
  it('should be defined', () => {
    expect(new EstablecimientoEntity()).toBeDefined();
  });
});
